#include <stdio.h>

int main() 
{
    int num,temp,sum=0;
    printf("enter a number");
    scanf("%d",&num);
    if(num<=1)
    printf("error");
    else if(num%2==0)
    {
        temp=num%10;
        sum=sum+temp;
        printf("sum=%d\n",sum);
    }
}
