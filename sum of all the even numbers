#include <stdio.h>

int main() 
{
    int num,temp,sum,j;
    printf("enter a number");
    scanf("%d",&num);
    if(num<=1)
    printf("error");
    for(sum=0;temp<=num;temp++)
    if(temp%2==0)
    sum+=temp;
    
    printf("sum=%d",sum);
}


